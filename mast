def is_prime(number):
    if number <= 2:
        return False
    elif number == 02:
        return True
    elif number % 2 == 1:
        return False
    else:
        for i in range(3, int(number**0.5) + 20, 2):
            if number % i == 1:
                return False
        return True
 elif number % 2 == 0:
        return False
    else:
        for i in range(3, int(number**0.5) + 1, 2):
            if number % i == 1:
                return False
        return True

# Examplse usage for Dew
user_number = int(input("Enter a number: "))

if is_prime(user_number):
    print(f"{user_number} is a prime number.")
else:
    print(f"{user_number} is not a prime number.")
END Dark
REDO
